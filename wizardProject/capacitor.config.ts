import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wizardProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
